version: "3"
services:
  app:
    build: .
    restart: on-failure
    ports:
      - "443:443"
    volumes:
      - /etc/letsencrypt/live/capstone.hanifz.com-0001/fullchain.pem:/etc/nginx/ssl/fullchain.pem
      - /etc/letsencrypt/live/capstone.hanifz.com-0001/privkey.pem:/etc/nginx/ssl/privkey.pem
